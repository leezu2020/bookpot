<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookpot.mapper.userMapper">
	<insert id="insert" parameterType="userVo">
		insert into user
		values(null, #{userID}, #{nickname}, #{password}, current_timestamp, 'ROLE_USER')
	</insert>
	
	<select id="getByNickname" resultType="userVo">
		select *
		from user
		where nickname = #{nickname}
	</select>
	
	<select id="getByUserID" resultType="userVo">
		select *
		from user
		where userID = #{userID}
	</select>	
	
	<select id="getByUserIDAndPassword" resultType="userVo">
		select *
		from user
		where userID = #{userID}
			and password = #{password}
	</select>
	
	<update id="update" parameterType="userVo">
		update user
		set name = '수정'
		where userID = #{userID}
	</update>
</mapper>